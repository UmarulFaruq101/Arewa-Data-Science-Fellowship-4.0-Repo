[project]
name = "project-folder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "UmarulFaruq101", email = "umarmuhammadsuleiman360@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.9",
    "pandas>=2.3.3",
]

[project.scripts]
project-folder = "project_folder:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
